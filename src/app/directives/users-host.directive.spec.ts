import { UsersHostDirective } from './users-host.directive';

describe('UsersHostDirective', () => {
  it('should create an instance', () => {
    const directive = new UsersHostDirective();
    expect(directive).toBeTruthy();
  });
});
